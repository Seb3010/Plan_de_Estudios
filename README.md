# Plan de Estudios — Sistema de Gestión de Estudiantes

¡Bienvenido! Proyecto personal diseñado para consolidar habilidades en lógica de programación, control de versiones con Git y manejo de archivos, para ser un futuro dev senior. Este es un proyecto desarrollado en **Python** para administrar información académica de alumnos de manera sencilla desde la consola.

## Estructura del repositorio

- `Flask/`: aplicación web ligera con `app.py` (API/servidor Flask)
- `Gestion_Estudiantil/`: aplicación de consola (`main.py`, `Estudiantes.py`, `materias.py`)

## Requisitos

- Python 3.11+ (se recomienda usar un entorno virtual)
- Para la app Flask: `pip install flask` (o usar `requirements.txt` si se añade)

## Cómo ejecutar

1. Crear y activar un entorno virtual desde la raíz del proyecto:

   - `python -m venv venv`
   - `source venv/bin/activate`

2. Ejecutar la app Flask (desde la carpeta `Flask`):

   - `cd Flask`
   - `python app.py`

3. Ejecutar la aplicación de consola (desde `Gestion_Estudiantil`):

   - `cd Gestion_Estudiantil`
   - `python main.py`

## Notas

- El proyecto es un ejemplo educativo; los datos se gestionan en memoria mediante los módulos existentes.
- Si quieres, puedo añadir un `requirements.txt` y comandos para ejecutar pruebas.

Hecho con 🐍 por **Sebastian Partida**
# Sistema de Gestión de Estudiantes